import { styled } from "styled-components";

export const Icons = styled.div`
    display: flex;
    column-gap: 12px;

    img {
        width: 30px;
    }
`

export const IconUsers = styled.div`
    display: flex;
    flex-direction: row;
    align-items: center;
    column-gap: 8px;

    p {
        font-size: 1.25rem;
        font-weight: 500;
    }
`